package pages;

import org.openqa.selenium.WebDriver;

public class PrescriptionPage extends ParentPage {
    public PrescriptionPage(WebDriver webDriver) {
        super(webDriver, "/prescription");
    }
}
