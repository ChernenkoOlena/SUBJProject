package pages;

import org.openqa.selenium.WebDriver;

public class DepilCatalogPage extends ParentPage {
    public DepilCatalogPage(WebDriver webDriver) {
        super(webDriver,"/catalog/depil");
    }
}
